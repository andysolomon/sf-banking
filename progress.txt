Banking App Salesforce Platform Engineering Project (#2) - Progress
Generated: 2026-06-16 EDT
Plan: docs/IMPLEMENTATION_PLAN.md
Sibling: projects/todo-app (v1.0.0, feature-complete) - this project INHERITS its pipeline,
         governance templates, scratch-org discipline, and free-tier org/limits knowledge.
Weighting: this project deepens the 3 JD areas todo-app left thin -
           integration patterns, reusable platform enablers, financial-data security.
           Core phases = 3, 6, 7, 9. The app is the cargo; the integration platform is the product.
Packages: frs-platform-core (base enabler) <- frs-banking (extension, depends on core).
Integration peer: frs-core-banking-mock (Next.js on Vercel, bidirectional + failure switch).

[~] 0.0 - Plan confirmed by Andy: chose to START this thread 2026-06-17 (scope/2-pkg/mock accepted).
          Folded in 2 study-derived additions: data-classification.md (Phase 3), daily reconciliation
          job (Phase 7). OPEN interactive call, DEFERRED to Phase 4: reuse todo-app DevHub/"prod" orgs
          (shared 6 scratch/day budget) vs fresh orgs. Not needed for Phases 1-3 (docs only).
[x] 1.0 - Phase 1 - Governance and Project Charter (inherit + adapt from todo-app) - DONE 2026-06-17
    [x] 1.1 - charter.md (two-team operating model, hats incl. Integration Architect + InfoSec, SoD)
    [x] 1.2 - change-management.md (+ Integration/Security change class with InfoSec gate; 2-pkg release rules)
    [x] 1.3 - definition-of-done.md (incl. "no secrets in source", HttpCalloutMock tests, data-classification)
    [x] 1.4 - ADR-001-methodology.md (hybrid reuse + platform-team-as-product / package-versioning framing)
[x] 2.0 - Phase 2 - Functional and Non-Functional Requirements - DONE 2026-06-17 (BASELINE SET)
    [x] 2.1 - functional-requirements.md (FR-1 balances, FR-2 transfer, FR-3/4 webhooks, FR-5 event, FR-6 flow)
    [x] 2.2 - non-functional-requirements.md (NFR-1 sec/least-priv, 2 idempotency, 3 resilience, 4 isolation,
              5 secrets, 6 SLA/circuit-breaker, 7 audit/reconciliation, 8 code quality, 9 reproducibility/2-pkg)
    [x] 2.3 - rtm.md skeleton (FR-1..6, NFR-1..9, all 'planned'; design col fills at Phase 3)
    [x] 2.4 - risk-register.md (13 risks; R-3..R-8 integration-focused: dup posting, core down, webhook spoof/replay, drift)
[x] 3.0 - Phase 3 - Integration & Security Architecture (CENTERPIECE) - DONE 2026-06-17
    [x] 3.1 - data-model.md (ERD mermaid; Bank_Account__c cached balance, Transaction__c LDV ledger,
              Transfer__c state machine; Integration_Log__c + Dead_Letter__c; money = Number(18,0) minor units)
    [x] 3.2 - security-model.md (OWD Private, sharing, FLS, 3 perm sets incl. least-priv integration user,
              access matrix, enforcement->verification map) + data-classification.md (4 tiers, per-field, redaction/Shield)
    [x] 3.3 - integration-architecture.md (pattern selection, callout framework, inbound, PE-vs-CDC, error
              taxonomy, monitoring/recon) + sequence-diagrams.md (4 mermaid flows: balance/transfer/webhook/event)
    [x] 3.4 - ADR-002-branching (reuse + 2-pkg specifics), ADR-003-two-package-architecture, ADR-004-integration-auth-pattern
    [x] 3.5 - RTM design column filled for all FR-1..6 + NFR-1..9; all rows now 'designed'
[~] 4.0 - Phase 4 - Source Control, Two-Package DX Project, and the Vercel Mock - NON-INTERACTIVE PARTS DONE 2026-06-17
    [x] 4.1 - DX project scaffold: sfdx-project.json (frs-platform-core + frs-banking, dep core<-banking, api 67.0),
              config/project-scratch-def.json, .forceignore, .gitignore, package source dirs w/ .gitkeep
    [~] 4.2 - LARGELY DONE 2026-06-17. ORG REALITY: orgfarm orgs NOT authed; used the connected **agentforce DevHub**
              (00DgK00000QYu0qUAD, alias DevHub) + prod = **prodtest** (00Dg500000BzwFGEAZ) per Andy's call.
              [x] Packages REGISTERED: frs-platform-core 0HogK00000038fNSAQ, frs-banking 0HogK00000038gzSAA (aliases in sfdx-project.json;
                  re-added banking default:true + dependency on core that `sf package create` had stripped).
              [x] GitLab project CREATED: gitlab.com/andysolomon/sf-banking (private); nested git repo; 67 files pushed to main [skip ci];
                  only_allow_merge_if_pipeline_succeeds=true; main protected (push/merge=maintainer).
              [ ] CI VARS PENDING ANDY (blocked for agent = credential harvesting): set SFDX_AUTH_URL_DEVHUB_B64 (masked, UNprotected)
                  + SFDX_AUTH_URL_PROD_B64 (masked, PROTECTED) from base64 of the org auth URLs. Commands handed off.
    [x] 4.3 - DONE: ci-templates/salesforce-multipackage-pipeline.yml (derived from sf-todo template, generalized:
              SF_PACKAGE_DIRS/SF_CORE_PACKAGE/SF_APP_PACKAGE/SF_PERMSETS; build core->banking, install core->banking)
              + .gitlab-ci.yml consumer (5 stages) + docs/devops/pipeline-design.md. YAML validated (ruby). Needs CI vars set (4.2).
    [x] 4.4 - frs-core-banking-mock DEPLOYED + LIVE 2026-06-17. Vercel project "mock" (prj_nxJBk3crONvNh5bX85uWLEF5V7wc,
              team_1h9Xca1fcyThRFdpK9runicq). Deployment URL: https://mock-9l74ecylz-andrewsolomonedus-projects.vercel.app
              (use the project's stable production alias for the Named Credential). ssoProtection DISABLED (was blocking with 401;
              PATCH needed teamId to persist). VERIFIED live: balance 200 JSON, transfer returns core_transfer_id, idempotency holds
              (same key -> same ct_ id), x-mock-fail:500 -> 500. Endpoints: GET /api/accounts/:id/balance, POST /api/transfers, POST /api/webhooks/send.
    [x] 4.5 - DONE: scripts/deploy-mock.sh; pipeline-design.md (above); .gitlab/merge_request_templates/Default.md
              (+ Integration-Security class, secrets/idempotency/callout-mock gates); docs/governance/code-review-checklist.md (integration section)
    [x] 4.6 - GATE DRILL via MR !1 (2026-06-23): PIPELINE FULLY GREEN — scan + lwc-test + apex-test all pass.
              apex-test in a FRESH scratch org: 7 tests, 100% pass, 95% org-wide coverage (gate cleared).
              CI vars set (Andy "U run them"). Fixed 4 CI bugs in sequence, each push revealing the next:
              (a) scan: 2 High violations — pmd ApexCRUDViolation (CalloutService insert → `insert as user`) +
                  eslint no-unused-vars (catch(e) → optional `catch {`). (b) login aborted: sf CLI 2.137 stdin
                  pipe → switched to --sfdx-url-file. (c) login didn't persist: node:22 "unsettled top-level
                  await" → pinned SF_CI_IMAGE=node:20. (d) apex-test create blocked by DailyScratchOrgs 0/6
                  (shared budget) → retried after reset (3/6) → green. The pipeline now authenticates, gates,
                  and proves the walking skeleton in a clean packaged org end to end.
    NOTE: deliberately NOT using Vercel Workflow for the mock's timeout-simulation (hook suggested it) - it's a
          stateless test double, not a durable process; bounded MOCK_DELAY_MS + maxDuration is the right fit.
[x] 5.0 - Phase 5 - Walking Skeleton - RELEASED v0.1.0 to prodtest 2026-06-23 (FR-1 live balance; full pipeline gated; break-glass prod install)
    [x] 5.1 - STAGED: frs-platform-core - CalloutService (generic Named-Cred GET + structured logging) + Integration_Log__c
              (4 fields) + CalloutServiceTest (success/error/throw via HttpCalloutMock)
    [x] 5.2 - STAGED: frs-banking - Bank_Account__c (6 fields) + BankAccountController.getLiveBalance (FLS, fail-soft) +
              bankAccountCard LWC (imperative, fail-soft) + Banking_Servicing_Agent perm set (read-only financial fields).
              Named Credential Core_Banking = POST-INSTALL (docs/devops/named-credential-setup.md), per ADR-004 (no secret in source).
    [x] 5.3 - VERIFIED vs devhub (free, no scratch): Apex 6/6 pass; bankAccountCard Jest 2/2 pass. branch feature/walking-skeleton-live-balance.
              Coverage gate + LIVE post-install smoke (docs/release/smoke-test-v0.1.0.md ready) PENDING package install.
    [x] 5.4 - PACKAGED + RELEASED v0.1.0 (2026-06-23). Earlier direct builds 0.1.0.1 (06-22); the MR-merge pipeline
              rebuilt 0.1.0.2 and that's what SHIPPED: frs-platform-core 0.1.0.2 = 04tgK000000DvlFQAS,
              frs-banking 0.1.0.2 = 04tgK000000DvoTQAS. Tag v0.1.0 on main @ 3917fb2. CHANGELOG [0.1.0] written.
              MAIN PIPELINE 2622737437: scan + lwc + apex-test (7/7, 95% cov) + package + staging-install ALL GREEN.
              PROD INSTALL = BREAK-GLASS direct CLI (manual prod-install job failed ci_quota_exceeded = GitLab compute
              minutes exhausted). Per drift-policy break-glass cycle, installed the SAME pipeline-built artifacts via
              sf package install (Andy-authorized, documented in CHANGELOG). PREREQ: uninstalled the platform-enablement
              sibling's frs-platform 0.2.0.1 (04tgK000000DvN3QAK) from prodtest first — both namespace-less + both define
              CalloutService → cannot coexist in one org (reinstallable; platform-enablement src/package untouched).
              POST-INSTALL DONE 2026-06-23: NC Core_Banking deployed to prodtest (legacy NoAuthentication/Anonymous) →
              STABLE url https://mock-andrewsolomonedus-projects.vercel.app. Smoke API checks PASS in prodtest: live
              NC→mock 200 ($1,323.19); Integration_Log__c Success/Outbound alias-only; mock fail switch x-mock-fail:500→500;
              no secret in installed source. RTM FR-1/NFR-5/NFR-6 → verified. ONLY remaining = Andy's UI sign-off (add
              bankAccountCard to the record page; confirm live render + fail-soft). PHASE 5 COMPLETE.
    [x] 5.3b - LIVE END-TO-END PROVEN 2026-06-22 (the walking skeleton's whole point). Could not scratch (3 active orgs from other
              projects = limit), so proved on DEVHUB (source already deployed): deployed a no-auth Named Credential Core_Banking ->
              the live Vercel mock; created Bank_Account__c(core_account_id='abc'); ran BankAccountController.getLiveBalance via anon
              apex -> REAL callout returned minor=132319 ($1,323.19, matches mock's deterministic value for 'abc'). Integration_Log__c
              logged Outbound/Success/Core_Banking/api/accounts/abc/balance (alias only, NO url/secret). Test data cleaned up.
              => FR-1 live-proven; NFR-5 (no secret in source) proven; platform-core logging proven in a real callout.
              REMAINING for v0.1.0 tag: install core->banking into prodtest + tag. PROPER path = pipeline (needs CI vars - Andy)
              OR explicit-authorized direct CLI install. NC is post-install (deployed ad hoc here for the proof; mock URL = the deployment hash URL).

    >>> RESUME PLAN — ✅ EXECUTED 2026-06-23 (see 5.4 above): CI vars set, MR !1 merged, pipeline green, v0.1.0 released. Kept below for the record. <<<
      PREREQ (Andy, the hard gate): set 2 CI vars on sf-banking. Run via `! ...` in-session or own terminal (agent BLOCKED = credential harvesting):
        cd ~/canvaser/projects/banking-app
        D=$(sf org display --target-org DevHub --verbose --json | python3 -c "import sys,json,base64;print(base64.b64encode(json.load(sys.stdin)['result']['sfdxAuthUrl'].encode()).decode())")
        echo "$D" | base64 -d | head -c 8   # must show force://  (else = sf 2.137 redaction gotcha, same as sf-todo)
        glab variable set SFDX_AUTH_URL_DEVHUB_B64 "$D" --masked              # UNprotected (MR pipelines need it)
        P=$(sf org display --target-org prodtest --verbose --json | python3 -c "import sys,json,base64;print(base64.b64encode(json.load(sys.stdin)['result']['sfdxAuthUrl'].encode()).decode())")
        glab variable set SFDX_AUTH_URL_PROD_B64 "$P" --masked --protected    # PROTECTED (main/hotfix only)
      THEN agent drives: open MR feature/walking-skeleton-live-balance -> main (scan/lwc-test/apex-test; do the 4.6 gate red->green drill on it)
        -> merge -> main pipeline builds both versions + staging-install (core->banking) -> MANUAL prod-install to prodtest -> tag v0.1.0.
      Already-built artifacts (pipeline can reuse or rebuild): core 04tgK000000DvIDQA0, banking 04tgK000000DvJpQAK.
      Scratch: full release needs 2 creates/day; active limit fine (2 used by hotel-booking+sfra-dev, expire 2026-06-27). Daily budget had 1 left
        on 2026-06-22 -> wait for reset. NC durability: use the mock's STABLE production URL (not the deployment-hash URL) for the real post-install NC.
    NOTE (2026-06-22): package VERSION CREATE caught 3 MORE real bugs offline+devhub checks missed (build org assigns NO perm set):
          (4) core had NO perm set -> CalloutServiceTest could not query Integration_Log__c fields. Added Platform_Core_Consumer perm set
              (the package now ships the grant for its OWN enabler objects); tests System.runAs a user holding it.
          (5) Banking_Servicing_Agent was read-only -> neither admin (no FLS in build org) nor agent (no create) could insert test data.
              Granted create+edit on operational fields (balance stays read-only) per security-model.md; test inserts AS the agent (owns it -> OWD Private satisfied).
          (6) `System.runAs([SELECT ...])` passes a List -> "runAs requires a single User"; assign to a User var first.
          KEY INSIGHT (interview gold): devhub tests can PASS on stale code / because admin has the perm set org-wide -> only a fresh build
          org (or inserting AS a perm-set-only user) reproduces the true packaged condition. "A package's tests must run as the user the
          PACKAGE grants, not the dev org's pre-configured admin."
    NOTE: First real compile (deploy to devhub) caught 3 genuine bugs the offline checks could not:
          (1) `currency` is an Apex RESERVED WORD -> BankAccountController would not compile. Renamed DTO prop to currencyCode;
              the core JSON key is literally "currency" so switched to JSON.deserializeUntyped (no typed member can be named currency).
          (2) .forceignore missing **/__tests__/** -> the Jest spec deployed into the LWC bundle -> LWC1702 (invalid createElement).
          (3) FLS GOTCHA: source/MDAPI deploys grant field-level security ONLY via the permset, NEVER to the SysAdmin profile (unlike
              the UI). So the running user saw "No such column" (SOQL) / "fields inaccessible" (DML) until the permset was assigned.
              The controller's WITH USER_MODE query makes this load-bearing. Proven fix = pipeline assigns SF_PERMSETS before apex tests
              (template already does this, matching sf-todo). Also fixed: consumer .gitlab-ci.yml listed Banking_Integration_User (does
              not exist till Phase 7) -> would break the CI permset-assign loop; trimmed to Banking_Servicing_Agent.
[x] 6.0 - Phase 6 - Reusable Platform-Core Package (THE ENABLER) - BUILT + GREEN on devhub 2026-06-25 (branch feature/phase6-platform-core)
    [x] 6.1 - Logger (Integration_Log__c + Category/Message/Retry_Count; payload-safe redact() masks digits/tokens). LoggerTest.
    [x] 6.2 - CalloutService matured: typed CalloutRequest/CalloutResult; send() + httpGet/httpPost; circuit-breaker (>=5 err/5min,
              fail-fast); Idempotency-Key header; async retry+exp-backoff via CalloutRetryQueueable; dead-letters on exhaustion.
    [x] 6.3 - PlatformEvents (EventBus wrapper + generic Integration_Event__e envelope) + Dead_Letter__c + DeadLetterService.replay
              (single, 1 callout/txn) + DeadLetterReplayBatch (scope 1 bulk). All logged via Logger.
    [x] 6.4 - frs-platform-core/README.md = explicit consumer API (install/permset/4 enablers/worked example/access-model/do-nots).
    [x] 6.5 - Mocked test suite: LoggerTest, CalloutServiceTest, DeadLetterServiceTest, PlatformEventsTest = 21 tests, 100% pass,
              92% org-wide coverage (every class >=82%, most 94-100%). Independent of frs-banking.
    ACCEPTANCE MET: frs-banking has ZERO direct HTTP code — BankAccountController.getLiveBalance now consumes CalloutService.httpGet
              (returns typed CalloutResult; fail-soft on !isSuccess incl. breaker). 3 real compile bugs caught+fixed on first deploy:
              (a) `like` is an Apex reserved word (var rename); (b) WITH USER_MODE must precede ORDER BY/LIMIT; (c) permset XML needs
              objectPermissions grouped contiguously. + design bug: replay() looped callouts (DML-between-callouts) -> Batchable scope 1.
    PENDING (not started this turn): deploy/package/release to prodtest (CI minutes exhausted; later MR) + Banking_Servicing_Agent
              gained the 3 new Integration_Log__c fields. NOT pushed/merged (feature branch only, validated on devhub).
[x] 7.0 - Phase 7 - Banking Integration Features + Failure Drills - BUILT+GREEN 2026-06-25 (branch feature/phase6-platform-core)
    [x] 7.1 - FR-2 Transfer BUILT+GREEN on devhub. Transfer__c (state machine Requested->Completed|Failed; Idempotency_Key__c UNIQUE
              = DB double-submit guard) + Transfer_Completed__e platform event. TransferService.initiate (idempotent pre-check + unique
              backstop; ASYNC via TransferCalloutQueueable - no callout in trigger/same txn). Queueable consumes CalloutService.send
              (idempotency key) -> success: Completed + PlatformEvents.publish(Transfer_Completed__e); transient: Failed + dead-letter
              (replayable); 4xx: Failed no dead-letter. ZERO HTTP code in frs-banking. TransferServiceTest 5/5 (blank-key, idempotent,
              success+publish, transient->dead-letter, 4xx). Full suite 61 tests 100%, 94% org-wide coverage.
    [x] 7.2 - FR-3/FR-4 inbound webhooks BUILT+GREEN on devhub. WebhookResource @RestResource (/core-banking/webhooks/*):
              defense-in-depth = (1) shared-secret X-Webhook-Token vs Webhook_Config__c hierarchy custom setting (value post-install,
              not in source); (2) replay guard = X-Event-Id recorded in Webhook_Event__c (UNIQUE) -> duplicate delivery => 200
              ignored, no reprocess; (3) field validation. Routes posted_transaction -> Transaction__c (ledger, External_Txn_Id__c
              UNIQUE) + fraud_alert -> Case. All logged Webhook/Inbound via Logger. NEW least-privilege permset Banking_Integration_User
              (the NFR-1 integration identity; re-add to SF_PERMSETS when packaging). WebhookResourceTest 7/7 (auth-401, posted-201,
              replay-200-idempotent, fraud->Case-201, missing-event-id-400, unknown/missing-txn-400). Full suite 68 tests 100%, 93% cov.
    [x] 7.3 - FR-5 Platform Event SUBSCRIBER BUILT+GREEN. Transfer_Completed_Trigger (after insert) -> TransferCompletedHandler
              projects each completed transfer into Transaction__c ledger (Debit, External_Txn_Id__c='xfer-'+coreId UNIQUE =>
              re-delivery idempotent via Database.insert allOrNone=false SYSTEM_MODE). Runs as Automated Process; ledger OWD Private
              so Banking_Integration_User got viewAllRecords on Transaction__c (reconciliation reads whole ledger). PE-vs-CDC trade-off
              documented in handler header (explicit event = stable contract, decoupled, simpler replay). Test 2/2. Full suite 71 100% 93%.
    [x] 7.4 - FR-6 Flow + batch jobs BUILT+GREEN. File_a_Dispute Screen Flow (input reason -> Create Case). StatementInterestBatch
              (bulkified, Schedulable; daily interest Credit per active +balance account; idempotent per acct/day via unique
              External_Txn_Id__c 'int-<acct>-<date>' + partial insert). ReconciliationBatch (Schedulable + AllowsCallouts, scope 1 =
              1 callout/txn; fetches LIVE core balance via CalloutService, compares to SF cached Balance_Minor__c, mismatch -> Case;
              NFR-7 safety net behind idempotency+retries). Tests 6/6. KEY LESSON: Balance_Minor__c is read-only to all interactive
              permsets (core-owned) -> tests seed it via Database.insert SYSTEM_MODE with OwnerId=agent (FLS-bypass but agent-owned so
              the batch WITH USER_MODE query sees it under OWD Private). Full suite 77 tests 100% 93% coverage.
    [x] 7.5 - DRILLS recorded in docs/release/integration-failure-drills.md. All 4 proven by the test suite (HttpCalloutMock =
              deterministic failure switch): (1) timeout/transient->retry->success; (2) 500->retry-exhaust->dead-letter->replay;
              (3) duplicate transfer blocked (unique idempotency key); (4) webhook replay rejected (unique X-Event-Id in Webhook_Event__c).
              Live mock switch (x-mock-fail:500->500) confirmed; manual live-drill procedure + drift safety net (ReconciliationBatch) documented.
[x] 8.0 - Phase 8 - Test, Quality, and Security Strategy - BUILT+GREEN 2026-06-26 (branch feature/phase6-platform-core)
    [x] 8.1 - docs/testing/test-strategy.md: the integration-app pyramid (HttpCalloutMock outbound matrix, RestContext inbound,
              EventBus/Batchable, security via runAs, Jest, Playwright-vs-installed-pkg+live-mock incl. forced-failure spec); the
              "failure switch = HttpCalloutMock not live", "run as the real persona", "describe FLS inside runAs" conventions; mock contract; CI gates.
    [x] 8.2 - SecurityModelTest 5/5: cross-customer ISOLATION (agent B cannot see agent A's Bank_Account, OWD Private) + positive
              control; balance read-only FLS (describe inside runAs + behavioral `update as user` blocked); integration-user LEAST-PRIV
              (no Bank_Account/Transfer access, can create Transaction). Negative-then-positive proofs.
    [x] 8.3 - RTM test column 100% (every FR-1..6 + NFR-1..9 now maps to a test/component; FR-1/NFR-5/6 verified, rest built).
              Coverage gate >=85% already enforced in CI (SF_COVERAGE_MIN=85 + test -ge). Full local suite 82 tests, 100%, 93% coverage.
[~] 9.0 - Phase 9 - Release Management & Integration Operations - DOCS DONE 2026-06-26; execution-gated items PENDING
    [~] 9.1 - release-runbook.md + rollback-runbook.md + hotfix-runbook.md WRITTEN (2-package: install core->banking, rollback
              banking->core reverse, dep pinning, protected hotfix/* ref). REHEARSALS pending (need orgs): rollback in-place
              pair-downgrade + record RTO; hotfix drill.
    [~] 9.2 - integration-failure-runbook.md WRITTEN (detect=breaker/logs/dead-letters -> degrade=fail-soft/queue -> recover=replay
              + reconcile -> comms). LIVE DRILL pending (devhub + live mock fail switch; record integration MTTR). Behaviour already
              proven deterministically in integration-failure-drills.md (7.5).
    [x] 9.3 - infosec-signoff.md DONE: trust boundary + data-classification table, controls-with-evidence matrix (maps each control
              to its test), residual risks (no-auth NC -> real External Cred for go-live), reviewer sign-off checklist. The
              "Partner with InfoSec" artifact.
    [x] 9.4 - salesforce-release-readiness.md DONE (seasonal impact scoped to the integration: API/NC/REST/PE/security/async + preview-org
              procedure + post-upgrade drift check). deployment-metrics.md DONE (DORA from real history: 1 release, 7+ defects caught
              pre-prod, gate-driven quality; integration MTTR pending the live drill).
    [ ] 9.5 - Release v1.0.0 - PENDING (needs CI minutes for the pipeline OR break-glass; then both packages tagged compatible,
              CHANGELOG, RTM frozen, post-release-review-v1.0.0.md). Blocked with Phase 6-8 on the same unpushed branch.

DEFERRED (post-v1.0.0): todo-app adopts frs-platform-core (consumer #2 = "multiple app teams" proof);
         Data Cloud (study-only); FSC, Shield enablement, real sandbox tiers, SAFe ceremonies.

CONSTRAINTS (carried from todo-app, still apply):
  - 6 scratch-org creates/day SHARED across local + CI AND now shared with todo-app - count before pushing (2-pkg builds burn more)
  - main protected = MR-only; merges via glab mr merge
  - SFDX auth URLs base64 in masked CI vars; never reuse unverified Thrivent metrics
  - do not push/merge/deploy (or deploy the Vercel mock) unless Andy explicitly asks

=== LATEST (2026-06-23) — v0.1.0 PIPELINE RELEASE: built+staging-proven via CI; PROD install blocked + rerouted ===
- MR !1 merged to main (3917fb2). Main pipeline GREEN through staging: scan, lwc-test, apex-test (scratch),
  VERSION built fresh 0.1.0.2 (core 04tgK000000DvlFQAS, banking 04tgK000000DvoTQAS), staging-install (core->banking
  into a scratch) all passed. prod-install reached the MANUAL gate.
- prod-install via CI BLOCKED: free-tier GitLab compute minutes EXHAUSTED (job pending 600s, runner:none, plan=free).
  Andy chose: complete prod via CLI. Job canceled.
- CLI install into prodtest FAILED — COMPONENT COLLISION: prodtest is the SHARED prod org (has todo-app 1.0.0.1) and
  the KEPT sibling project platform-enablement already installed its package **frs-platform 0.2.0.1**
  (0HogK00000038c9SAA / SPV 04tgK000000DvN3QAK) which contains a CalloutService class -> banking-app's frs-platform-core
  cannot own the same-named class in the same org.
- DECISION (Andy): give banking-app its OWN prod org (separate from platform-enablement's frs-platform in prodtest).
  PENDING ANDY (interactive): create a fresh Developer Edition org + `sf org login web --alias banking-prod`.
  THEN agent: install core->banking (use the PIPELINE-built 0.1.0.2: 04tgK000000DvlFQAS then 04tgK000000DvoTQAS) into
  banking-prod, tag v0.1.0, advance RTM. Andy also re-points CI var SFDX_AUTH_URL_PROD_B64 -> banking-prod for future pipeline prod releases.
- LESSON (interview gold): reusing a SHARED prod org across two platform-style projects collided on a duplicated component
  (CalloutService). Real fix = org-per-product OR a single shared platform package. Plus: free-tier CI minutes are a finite
  shared resource across a namespace — a 16-min package build can exhaust the monthly pool mid-release.
